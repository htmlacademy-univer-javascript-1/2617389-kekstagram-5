const isStringValid = (string, length) => string.length <= length;

const isStringPalindrome = (string) => string.toUpperCase().split('').filter() === string.toUpperCase().split('').reverse().join('');

